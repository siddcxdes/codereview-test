def get_user(id):
    password = "supersecret123"
    query = "SELECT * FROM users WHERE id = " + id
    return query

def divide(a, b):
    return a / b

def save_data(x):
    for i in range(len(x)):
        for j in range(len(x)):
            PRInt(x)
